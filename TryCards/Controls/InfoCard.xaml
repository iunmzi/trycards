<UserControl x:Class="TryCards.Controls.InfoCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">
    <Grid>
        <Border BorderBrush="DarkBlue" BorderThickness="1" Margin="2"
                Grid.Column="0" Grid.Row="0" Grid.RowSpan="3"
                CornerRadius="5" Opacity="0.5"/>
        <Grid Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>



            <Label Grid.Row="0" FontSize="20" FontWeight="Bold">Block Name</Label>
            <Label Grid.Row="1" Foreground="Blue" FontStyle="Italic">Path on disc or network</Label>
            <Label Grid.Row="2">Extra info</Label>
        </Grid>
    </Grid>

</UserControl>
